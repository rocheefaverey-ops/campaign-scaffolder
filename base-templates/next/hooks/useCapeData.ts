'use client';

// Re-export from the canonical provider so imports are consistent:
//   import { useCapeData } from '@hooks/useCapeData'
// or
//   import { useCapeData } from '@components/providers/CapeDataProvider'
export { useCapeData } from '@components/providers/CapeDataProvider';
