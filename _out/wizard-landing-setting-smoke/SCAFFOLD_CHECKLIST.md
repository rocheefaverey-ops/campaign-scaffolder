# Scaffold Checklist — wizard-landing-setting-smoke

> Generated 2026-05-07 08:26:25 UTC


## Project

- **Name:** wizard-landing-setting-smoke
- **Stack:** Next.js 16 App Router  *(Front-end based)*
- **CAPE ID:** 123
- **Market:** NL
- **Engine:** unity
- **Reg mode:** after

---

## Pages & Routes

- [x] **landing** — `/landing`
- [x] **onboarding** — `/onboarding`
- [x] **game** — `/gameplay`
- [x] **result** — `/result`

### Menu items

> The menu only links to generated routes or configured external legal URLs.

- [x] **home** — `/landing`
- [ ] **resume** — disabled
- [x] **howToPlay** — `/onboarding`
- [ ] **leaderboard** — disabled; `/leaderboard` was not generated
- [ ] **voucher** — disabled; `/voucher` was not generated
- [x] **terms** — `general.legal.termsUrl`
- [x] **privacy** — `general.legal.privacyUrl`
- [ ] **faq** — disabled; `/faq` was not generated
- [x] **leave** — `/`

### Computed flow tokens

> These are injected into the generated files. Verify the routing matches your campaign flow.


- `{{FLOW_ENTRY}}` → `/landing`
- `{{NEXT_AFTER_LANDING}}` → `/onboarding`
- `{{NEXT_AFTER_ONBOARDING}}` → `/gameplay`
- `{{NEXT_AFTER_GAME}}` → `/result`
- `{{NEXT_AFTER_RESULT}}` → `/landing`
- `{{PLAY_AGAIN_ROUTE}}` → `/gameplay`
- `{{LANDING_LEADERBOARD_ROUTE}}` → `/landing`
- `{{RESULT_LEADERBOARD_ROUTE}}` → `/landing`

---

## Modules

### Installed

- [x] unity
- [x] scoring

### Module checklist

- [ ] [scoring] Confirm `API_URL` create-session and end-session paths match backend
- [ ] [scoring] Test: `create-session` action fires on game start
- [ ] [scoring] Test: `end-session` action fires on game end with correct score

---

## Environment Variables

> `.env` was created from `env.dist` and points at the real CAPE acceptance CDN. Fill in `API_URL`, `SERVER_SECRET`, and any module-specific vars before running.


### Always required

- [ ] Set `NEXT_PUBLIC_CAPE_DEFAULT_ID` = `123`
- [ ] Set `NEXT_PUBLIC_CAPE_DEFAULT_MARKET` = `NL`
- [ ] Set `API_URL` to your backend base URL
- [ ] Set `SERVER_SECRET` to a random secret (32+ chars)

### Unity (needs filling)

- [ ] Set `UNITY_BASE_URL` (or `NEXT_PUBLIC_UNITY_BASE_URL`) to your GCS CDN build path
- [ ] Set `UNITY_GAME_NAME` (or `NEXT_PUBLIC_UNITY_GAME_NAME`) to your Unity output folder name

### Added by modules

- [ ] Set `NEXT_PUBLIC_UNITY_BASE_URL`
- [ ] Set `NEXT_PUBLIC_UNITY_GAME_NAME`
- [ ] Set `NEXT_PUBLIC_UNITY_MIN_DPR`
- [ ] Set `NEXT_PUBLIC_UNITY_MAX_DPR`

---

## CSP Patches Applied

> These were written into `proxy.ts`. Verify they are correct for your environment.


### unity

- [x] `script-src` ← `https://*.lwcf.nl`
- [x] `script-src` ← `https://*.lwcf5.nl`
- [x] `script-src` ← `https://lw-*.lwdev.nl`
- [x] `frame-src` ← `'self'`
- [ ] [unity] Test: no CSP violations in browser console

---

## CAPE Integration

> Pull manually from `lwg-cli-cape` to inspect the live campaign JSON.


### 1 — Push the generated format

> `cape-format.json` was generated with all pages, elements, and modules. Run these once from `lwg-cli-cape`:


```bash
node cli.js push-format "C:/Dev/Livewall/campaign-scaffolder/_out/wizard-landing-setting-smoke.scaffold-tmp-1778142382134/cape-format.json" 123
node cli.js populate-defaults 123
node cli.js publish 123
```

- [ ] Run the three commands above from `lwg-cli-cape`
- [ ] Verify CAPE format has all expected pages/tabs

### 2 — Fill in content

> Fill in copy, upload images, and configure settings in CAPE:


- [ ] Verify campaign `123` is published to **acceptance** before testing
- [ ] Run `node cli.js fetch 123` from `lwg-cli-cape` to inspect campaign data
- [ ] Confirm CAPE design tokens / copy match the campaign brief

---

## Smoke Tests

> Run `npm run dev:full-mock` to test everything locally without external dependencies (no backend, no CAPE CDN, no Unity CDN needed).


### General

- [ ] `npm run dev:full-mock` starts without errors  *(uses mock API + mock CAPE + mock game bridge)*
- [ ] `npm run dev` starts without errors  *(requires real .env values)*
- [ ] No TypeScript errors: `npm run ts-compile` (or `npx tsc --noEmit`)
- [ ] No console errors on first page load
- [ ] Navigation flows through all pages in correct order

### Per-page

- [ ] `/landing` — loads without error
- [ ] `/onboarding` — loads without error
- [ ] `/gameplay` — loads without error
- [ ] `/result` — loads without error

---

## Git

- [x] Initial scaffold commit created
- [ ] Push to remote origin
- [ ] Open a PR / branch for campaign-specific development

---

*Generated by Livewall Campaign Scaffolder — 2026-05-07 08:26:25 UTC*
